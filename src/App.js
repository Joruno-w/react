import React, {Component} from 'react';
import OldContext from "./OldContext";
class App extends Component {
    render() {
        return (
            <OldContext />
        );
    }
}

export default App;
